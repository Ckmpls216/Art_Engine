"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CanvasImageProcessor = void 0;
const canvas_1 = __importStar(require("canvas"));
const fs = __importStar(require("fs"));
class CanvasImageProcessor {
    cachedAssets;
    constructor() {
        this.cachedAssets = {};
    }
    async createImageWithLayers(createImageWithLayersProps) {
        let canvas = canvas_1.default.createCanvas(createImageWithLayersProps.width, createImageWithLayersProps.height);
        const ctx = canvas.getContext("2d");
        for (let asset of createImageWithLayersProps.assets) {
            if (!this.cachedAssets[asset.path]) {
                this.cachedAssets[asset.path] = await (0, canvas_1.loadImage)(asset.path);
            }
            ctx.drawImage(this.cachedAssets[asset.path], asset.xOffset, asset.yOffset);
        }
        fs.writeFileSync(createImageWithLayersProps.outputPath, canvas.toBuffer("image/png"));
    }
}
exports.CanvasImageProcessor = CanvasImageProcessor;
